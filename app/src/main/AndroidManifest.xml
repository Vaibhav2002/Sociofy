<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vaibhav.sociofy">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ui.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sociofy">
        <activity android:name=".ui.settingScreen.SettingsActivity"></activity>
        <activity android:name=".ui.UserListScreen.UserListActivity" />
        <activity android:name=".ui.ProfileScreen.ProfileActivity" />
        <activity android:name=".ui.UploadScreen.UploadActivity" />
        <activity android:name=".ui.authorization.AuthActivity" />
        <activity android:name=".ui.SplashScreen.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.HomeScreen.HomeActivity" />
    </application>

</manifest>